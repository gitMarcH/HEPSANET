model{

# model
for(j in 1:N){
  y[j]~dbern(p[study[j],state[j]])
}

for(i in 1:m){
  p[i,1]=p_sens[i] # state 1 = diseased / positive
  p[i,2]=1-p_spec[i] # state 2 = healthy / negative
  
  p_sens[i]~dbeta(0.5,0.5)
  p_spec[i]~dbeta(0.5,0.5)
}

}
